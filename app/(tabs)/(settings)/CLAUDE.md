# (settings)/
> L2 | Parent: app/(tabs)/CLAUDE.md

Settings tab — Apple grouped list hub with pushed sub-pages for each settings domain.

## Members

```
_layout.tsx:        Stack navigator — large title on index, material blur on pushed sub-pages
index.tsx:          Settings hub — account card, sync controls, navigation rows to sub-pages
account.tsx:        Account sub-page — profile summary, linked accounts, sign out, delete account
appearance.tsx:     Appearance sub-page — theme mode segmented control, name font picker
notifications.tsx:  Notifications sub-page — push notification type toggles
privacy.tsx:        Privacy & Sharing — default version, QR toggle, contact preferences, share history
about.tsx:          About sub-page — app info, legal links, support actions
```

[PROTOCOL]: Update this on any file add/remove/rename, then check app/(tabs)/CLAUDE.md
