# api/src/
> L2 | Parent: api/CLAUDE.md

Express application core — server setup, route mounting, and business logic services.

## Members

```
index.ts:       Express app — cors, JSON, request logging, route mounting, health check, 404/error handlers
routes/         HTTP route handlers (linkedin, share, wallet, emoji)
services/       Business logic (scraper, passGenerator, ai, emoji)
```

[PROTOCOL]: Update this on any file add/remove/rename, then check api/CLAUDE.md
