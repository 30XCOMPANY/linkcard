# src/
> L2 | Parent: /CLAUDE.md

Application source — components, services, stores, design system, and utilities.

## Members

```
components/     — Shared UI components (card, shared, ui, modals, qr)
css/            — Tailwind v4 CSS foundation (sf.css, glass.css, global entry)
design-system/  — Unified design tokens, primitives, patterns, layouts
lib/            — Core utilities (cn, haptics, springs, accent-colors, icons)
services/       — Backend integration (Supabase, LinkedIn, share, wallet, sync)
stores/         — Zustand state management (cardStore)
tw/             — CSS wrapper layer (useCssElement bridges for className/Tailwind)
types/          — TypeScript interfaces (LinkedInProfile, CardVersion, etc.)
```

[PROTOCOL]: Update this on any file add/remove/rename, then check /CLAUDE.md
