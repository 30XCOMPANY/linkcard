# stores/
> L2 | Parent: /CLAUDE.md

Zustand state management with AsyncStorage persistence.

## Members

```
cardStore.ts:  Main app state — card data, theme mode, accent color, gradient.
               Debounced Supabase sync (500ms). Persists card, theme, accent, gradient to AsyncStorage.
```

[PROTOCOL]: Update this on any file add/remove/rename, then check /CLAUDE.md
