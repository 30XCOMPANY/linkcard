# card/
> L2 | Parent: src/components/CLAUDE.md

Pure presentational card rendering. These files compose the business-card surfaces and hero profile view without touching stores directly.

## Members

- `CLAUDE.md`: This local member map for card renderers
- `card-field.tsx`: Single field renderer — editorial typography map (extreme weight contrast), applies FieldStyle overrides as inline style
- `card-display.tsx`: Editorial card renderer — compact/full display surface with avatar, QR overlay, and version chrome
- `profile-card.tsx`: Profile hero card — banner-backed profile view with glass-framed avatar, tags, links, and publications

[PROTOCOL]: Update this header on change, then check CLAUDE.md
