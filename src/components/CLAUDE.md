# components/
> L2 | Parent: /AGENTS.md

Presentational UI modules consumed by screens and flows. No store logic lives here; composition stays in app screens.

## Members

- `CLAUDE.md`: This local module map — documents `card/` and `shared/`
- `card/`: Card surfaces and editorial renderers — stateless business-card composition via props
- `shared/`: Reusable primitives — guarded glass, avatar renderer, QR wrapper

[PROTOCOL]: Update this header on change, then check CLAUDE.md
