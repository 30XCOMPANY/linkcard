# design-system/
> L2 | Parent: src/CLAUDE.md

Unified design tokens, primitives, patterns, and layouts. Single source of truth for all visual tokens.

## Members

```
settings.tsx:   iOS grouped settings surfaces — SettingsGroup, SettingsRow, SettingsAccountCard,
                SettingsSegmented, SettingsColorGrid, SettingsChevron, SettingsIconTile, settingsPageStyle
tokens/         Colors, typography, spacing, radii, shadows, effects, animation, theme
primitives/     Box, Text, VStack, HStack
patterns/       Button, Input, GlassCard, GlassButton
layouts/        GlassScreenLayout
theme/          ThemeProvider + useTheme (Apple HIG semantic colors)
bento/          BentoGrid layout primitives
```

[PROTOCOL]: Update this on any file add/remove/rename, then check src/CLAUDE.md
