/**
 * [INPUT]: ./ThemeProvider
 * [OUTPUT]: ThemeProvider, useTheme, ThemeValue
 * [POS]: Theme barrel
 * [PROTOCOL]: Update this header on change, then check CLAUDE.md
 */

export { ThemeProvider, useTheme } from './ThemeProvider';
export type { ThemeValue } from './ThemeProvider';
